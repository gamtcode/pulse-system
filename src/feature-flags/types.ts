/**
 * Type definitions for feature flags.
 * Centralizes all flag keys used in the application.
 */
export interface FeatureFlags {
    newDashboard: boolean;
    betaCharts: boolean;
    ui_badge_enabled: boolean;
}
